{
  "private": true,
  "_comment": "gsap/lenis are declared here only as the source for the vendored browser copies in js/vendor/ (gsap@3.15.0, lenis@1.3.25). They are NOT imported from node_modules at runtime. @vercel/blob is used by api/manifest.js. package-lock.json pins these versions.",
  "scripts": {
    "build:nav": "node scripts/build-nav.mjs"
  },
  "dependencies": {
    "@vercel/blob": "^2.6.1",
    "gsap": "^3.15.0",
    "lenis": "^1.3.25"
  }
}
